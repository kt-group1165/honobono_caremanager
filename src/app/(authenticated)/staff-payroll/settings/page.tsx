import { WageSettingsContent } from "./settings-content";

/**
 * /staff-payroll/settings — パート給与 サービス類型・時給・割当の設定 (事業所別)
 */
export default function WageSettingsPage() {
  return <WageSettingsContent />;
}
