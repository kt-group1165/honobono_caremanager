import { SeikyuContent } from "./seikyu-content";

/**
 * /billing-visit/seikyu — 請求 (1 画面タブ切替)
 * 月次情報 / 介護請求 / 利用請求 / 国保請求 をタブで切替える統合画面。
 */
export default function SeikyuPage() {
  return <SeikyuContent />;
}
